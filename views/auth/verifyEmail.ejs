<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>VERIFY EMAIL</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <div class="container">
      <%- include('../header') -%>
      <h1 style="text-align: center">Verify Email</h1>
      <main
        style="
          box-shadow: 0px 0px 5px #9f9f9f;
          max-width: 650px;
          margin: 0 auto;
        "
      >
        <% if(user) { %>
        <p
          style="
            text-align: left;
            background-color: rgba(156, 251, 156, 0.517);
            color: green;
            margin-bottom: -10px;
            padding-bottom: 20px;
          "
        >
          You are logged In
        </p>
        <%} else { %>
        <p style="text-align: center">You are not logged in</p>
        <% } %>
        <div style="max-width: 650px; margin: 20px;">
            <p><b>Email</b> <%= email || "" %></p>
            <p>Your email has not been verify youremail by entering the 8-digit cde or request a new verification link</p>
            <a href="/resend-verification">Resend Verification Link</a>
        </div>
        <form action="verify-email-token" method="get">
          <div>
            <input type="text" name="token" placeholder="Enter 8-digit Verification Code" >
           <button class="btn" type="submit">Verify Code</button>
          </div>
        </form>
      </main>

      <%- include('../footer') -%>
    </div>
  </body>
</html>
